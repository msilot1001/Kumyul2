import IBotConfig from '../Interfaces/IBotConfig.js';

// 커스텀 가능한 Config
const BotConfig: IBotConfig = {
  // 메세지 커맨드 활성화 (기본: 비활성화) (비권장)
  enableMessageCommand: false,
};

export default BotConfig;
